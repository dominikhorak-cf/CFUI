import { CardProps } from './Card.types';
export default function Card({ name, type, role, className, required, disabled, placeholder, values, defaultValue, icon, ...props }: CardProps): import("react/jsx-runtime").JSX.Element;
