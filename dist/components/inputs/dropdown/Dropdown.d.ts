import { DropdownProps } from './Dropdown.types';
export default function Dropdown<T>({ name, values, defaultValue, output, label, placeholder, disabled, allowSearching, selectMultiple, allowDeselecting, onSelect, className, positions, align, }: DropdownProps<T>): import("react/jsx-runtime").JSX.Element;
